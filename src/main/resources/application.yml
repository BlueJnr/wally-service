spring:
  datasource:
    url: jdbc:h2:mem:wallydb
    driverClassName: org.h2.Driver
    username: wally
    password: yllaw
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true
springdoc:
  api-docs:
    path: /api-docs
  override-with-generic-response: false
